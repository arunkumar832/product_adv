{
  "name": "server",
  "version": "1.0.0",
  "description": "backend server for gatsby app",
  "main": "src/app.js",
  "scripts": {
    "start": "export NODE_OPTIONS=--openssl-legacy-provider && ./node_modules/.bin/netlify-lambda serve src",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && ./node_modules/.bin/netlify-lambda build src"
  },
  "author": "Arunkumar G",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.1",
    "express": "^4.17.2",
    "netlify-lambda": "^2.0.15",
    "serverless-http": "^2.7.0"
  }
}
